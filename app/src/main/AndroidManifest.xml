<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.loopbreakr.cogstruct">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CogStruct">
        <activity android:name=".badbehaviors.activities.BBActivity"></activity>
        <activity android:name=".howdigethere.activities.HIGHActivity" />
        <activity android:name=".identifybarriers.activities.IBActivity" />
        <activity android:name=".proscons.activities.PCActivity" />
        <activity android:name=".logs.activities.LogsActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".thoughtjournal.activities.TJActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>